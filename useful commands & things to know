# Install NPM
winget install Schniz.fnm

fnm install 22

node -v

npm -v

# Firebase Set Up On Workstation
npm install -g firebase-tools

# Firebase Log In
firebase login

# Install Astro
npm install -g create-astro

# Install Astro in Project
npm install astro

# Local Server for testing
npm run dev

# Create build, in lower case leaderboarded
npm run build

# Deploy to firebase, in upper case Leaderboarded
firebase deploy

-----------

Build project in leaderboarded/src

Images can go in leaderboarded/public